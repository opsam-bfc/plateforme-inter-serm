streamlit>=1.48.0
pandas>=2.0
geopandas>=1.0
# pyyaml : lecture du fichier de configuration du territoire (config/territoire.yaml)
pyyaml>=6.0
shapely>=2.0
# Plotly 7.0 supprime les traces mapbox (scattermapbox, choroplethmapbox)
# et le sous-graphe mapbox, utilises par toutes les cartes de la plateforme.
# Migration vers les traces MapLibre (*map) requise avant de lever la borne.
plotly>=6.0,<7.0
reportlab>=4.0
# Kaleido 0.2.x embarque le moteur Chrome -> indispensable pour fig.to_image()
# (export PDF des graphiques Plotly sans Chrome, compatible Streamlit Cloud).
kaleido>=0.2.1,<1.0.0
pypdf>=4.0
openpyxl>=3.1
Pillow>=10.0
# pyarrow : lecture Parquet (données tabulaires + GeoParquet réseau optimisé)
pyarrow>=15.0
requests>=2.31
